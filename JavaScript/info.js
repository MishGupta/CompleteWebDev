//prompt("") is used for the message where user can enter the message.
//slice is used for viewing only those things which we want like characyters from (0,2) in Mishka so only Mi will be visible
//toUpperCase is used for viewing the words in upper case
//toLowerCase is used for viewing the words in lower case
//== is used for just comparing whereas === is used for comparing the datatype also
//output.push() is used for pushing a item in an array at the last of the list
//name_of_arr.pop() removes the items from array